/**
 * Returns the API base URL.
 * Uses relative paths so it works on both localhost and production (Vercel).
 *
 * IMPORTANT: Never hardcode http://localhost:3000 in fetch calls.
 * Always use relative paths like: fetch('/api/m2m/feed')
 */
function getApiBase() {
    return '';  // Always use relative paths — works everywhere
}
