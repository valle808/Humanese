[project]
name = "chatbot-python"
version = "0.0.1"
description = "CDP AgentKit Autogen Chatbot Example"
authors = [{ name = "Taejin Kim", email = "kitto727@gmail.com" }]
requires-python = "~=3.10"
readme = "README.md"
dependencies = [
    "coinbase-agentkit",
    "coinbase-agentkit-autogen",
    "python-dotenv>=1.0.1,<2",
    "autogen-agentchat>=0.7.1,<0.8",
    "autogen-ext[openai]>=0.7.1,<0.8"
]

[dependency-groups]
dev = ["ruff>=0.7.1,<0.8"]

[tool.uv]
package = false

[tool.uv.sources]
coinbase-agentkit = { path = "../../../python/coinbase-agentkit", editable = true }
coinbase-agentkit-autogen = { path = "../../../python/framework-extensions/autogen", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.autogen-cdp-chatbot]
private = true

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["./build/**", "./dist/**", "./docs/**"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "D", "UP", "B", "C4", "SIM", "RUF"]
ignore = ["D213", "D203", "D100", "D104", "D107", "E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ruff.lint.isort]
known-first-party = ["coinbase_agentkit, cdp"]
