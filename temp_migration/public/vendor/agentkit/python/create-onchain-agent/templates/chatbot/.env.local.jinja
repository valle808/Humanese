{% if _wallet_provider == "server"%}# Required
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=
OPENAI_API_KEY=

# Optional with defaults
NETWORK_ID=base-sepolia

# Optional - Only one of these should be set:
# Set ADDRESS if you have an existing wallet
ADDRESS=

# Optional - Set IDEMPOTENCY_KEY to create a new wallet and seed the randomness
IDEMPOTENCY_KEY={% endif %}{% if _wallet_provider == "smart"%}# Required
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=
OPENAI_API_KEY=

# Optional with defaults
NETWORK_ID=base-sepolia

# Optional - Only one of these should be set:
# Set SMART_WALLET_ADDRESS if you have an existing wallet. If not, a new one will be created
SMART_WALLET_ADDRESS=

# Optional - Set OWNER_SERVER_WALLET_ADDRESS if you have an existing CDP server wallet address you wish to use as the owner
# If neither OWNER_SERVER_WALLET_ADDRESS nor OWNER_PRIVATE_KEY are set, a new server wallet will be created as the owner
OWNER_SERVER_WALLET_ADDRESS=

# Optional - Set IDEMPOTENCY_KEY to seed the randomness of a new server wallet if OWNER_SERVER_WALLET_ADDRESS and OWNER_PRIVATE_KEY are not set
OWNER_IDEMPOTENCY_KEY=

# Optional - Set OWNER_PRIVATE_KEY if you have an existing private key you wish to own the smart wallet
OWNER_PRIVATE_KEY=

# Optional - Set PAYMASTER_URL to sponsor transactions: https://docs.cdp.coinbase.com/paymaster/docs/welcome
PAYMASTER_URL={% endif %}{% if _wallet_provider == "eth"%}# Required
CDP_API_KEY_ID= # Place your CDP API key id here
CDP_API_KEY_SECRET= # Place your CDP API key secret here
OPENAI_API_KEY= # Place your OpenAI API key here

# Optional - Set PRIVATE_KEY if you have an existing private key. Otherwise, a new account will be created.
PRIVATE_KEY=

# Optional - Set CHAIN_ID to select your chain. Otherwise, the default Base Sepolia chain ID will be used.
CHAIN_ID=
{% endif %}{% if _wallet_provider == "solana_server"%}# Required
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=
OPENAI_API_KEY=

# Optional with defaults
NETWORK_ID=solana-devnet

# Optional - Only one of these should be set:
# Set ADDRESS if you have an existing Solana wallet
ADDRESS=

# Optional - Set IDEMPOTENCY_KEY to create a new wallet and seed the randomness
IDEMPOTENCY_KEY={% endif %}
